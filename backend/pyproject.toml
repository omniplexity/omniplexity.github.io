[project]
name = "omniai-backend"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.109",
  "uvicorn>=0.27",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
