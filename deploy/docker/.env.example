# OmniAI Docker Environment (Phase 1)
# Copy to .env and edit values. NEVER commit .env.

ENV=prod

# CORS allowlist (comma-separated)
CORS_ORIGINS=https://omniplexity.github.io,http://localhost:5173

# Auth (Bearer tokens recommended for GitHub Pages)
AUTH_MODE=bearer
JWT_SECRET=change_me
JWT_ACCESS_TTL_SECONDS=900

# Model selection defaults (optional)
DEFAULT_PROVIDER=
DEFAULT_MODEL=
PROVIDER_PRIORITY=lm_studio,ollama,openai_compat
MODEL_PRIORITY=qwen,deepseek,llama,gpt

# Cloudflare Tunnel (named tunnel token)
CLOUDFLARE_TUNNEL_TOKEN=

# ngrok (fallback tunnel)
NGROK_AUTHTOKEN=

# Session secrets (required for current backend until bearer auth is implemented)
SECRET_KEY=change_me
CSRF_SECRET=change_me
ADMIN_BOOTSTRAP_TOKEN=change_me

# Origin lock (recommended for tunnel-only access)
ORIGIN_LOCK_ENABLED=true
ORIGIN_LOCK_SECRET=change_me

# Storage
DATA_DIR=/data
UPLOAD_DIR=/uploads

# Public API hostname used by the frontend runtime config
API_PUBLIC_ORIGIN=https://YOUR_PUBLIC_API_HOSTNAME
